import { ReactElement } from "react"

export const Logo = (): ReactElement => {
  return (
    <p className="font-source text-4xl text-dark">
      <b>
        <span className="font-serif">y</span>um
      </b>
      <span>mily</span>
      <span className="text-orange">.</span>
    </p>
  )
}
