import { ReactElement } from "react"

export const Logo = (): ReactElement => {
  return (
    <h1 className="font-source text-4xl text-dark">
      <b>
        <span className="font-serif">y</span>um
      </b>
      <span>mily</span>
      <span className="text-orange">.</span>
    </h1>
  )
}
